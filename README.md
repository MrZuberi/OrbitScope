# OrbitScope

OrbitScope is an interactive 3D solar system simulator built in C++ and OpenGL.

## Requirements

CMake 3.20 or newer and a C++17 compiler.

## Building

Configure and build with CMake. GLFW and GLM are fetched automatically during configuration.